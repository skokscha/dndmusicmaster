# Решения

Дата | Решение | Причина
--- | --- | ---
2026-09-24 | Версии: AGP 9.4.1, Kotlin 2.4.20, Gradle 9.7.1, Compose BOM 2026.09.00, JDK 21 (компиляция в 17), compileSdk/targetSdk 37, build-tools 37.0.0, JUnit 6.1.3 | Проверены по Google Maven / Maven Central / services.gradle.org в день начала проекта; AGP 9 требует JDK 17+ и Gradle ≥ 9.6
2026-09-24 | DI — ручной (без Koin/Hilt), пока приложение маленькое | Для двух модулей ручной DI проще и без кодогенерации; Koin добавим, если граф вырастет
2026-09-24 | AGP 9 встроенный Kotlin: плагин `org.jetbrains.kotlin.android` не подключается, только `kotlin.plugin.compose` | Так работает AGP 9.x; проверено на рабочем примере
2026-09-24 | `:core` тестируется на JUnit 6 (преемник JUnit 5, `useJUnitPlatform`) | JUnit 5 переименован в 6.x; API Jupiter тот же
2026-09-24 | compileSdk/targetSdk = 37, пакет SDK называется `platforms;android-37.0` (новая схема именования с extension-суффиксом) | Проверено через `sdkmanager --list`; AGP 9.4 поддерживает максимум API 37
2026-09-24 | Этап 3: Room 2.8.5 через KSP 2.3.12, DataStore 1.2.1 | Версии проверены по Google Maven в день подключения; KSP перешёл на одиночную нумерацию (без `kotlinVersion-`), 2.3.x официально поддерживает AGP 9 built-in Kotlin — проверено сборкой
2026-09-24 | Индекс библиотеки — одна таблица `indexed_files` (путь = PK), meta.json хранится текстом в той же таблице | Пересборка Library идёт из кэша Room без SAF; текст meta.json нужен, чтобы оверрайды выживали между перезапусками; избранное/оверрайды позиций — отдельные таблицы, скан их не трогает
2026-09-24 | `Library` пересобирается чистой функцией `LibraryIndexBuilder` в `:core` из плоского списка файлов | Вся логика соглашений покрыта unit-тестами без Robolectric; SAF-сканер и Room только доставляют данные
2026-09-24 | Обход дерева — напрямую `DocumentsContract` (BFS-очередь), не `DocumentFile` | DocumentFile создаёт каскад resolver-запросов на каждый файл и слишком медленный для больших библиотек
2026-09-24 | Длительности ищутся `MediaMetadataRetriever` пакетами по 32 файла за скан и кэшируются в Room | Первый скан остаётся отзывчивым; дальше длительность не пересчитывается, пока не изменились размер+mtime
2026-09-24 | Онбординг/отладочный экран (этап 3) заменяет заглушку; трёхпанельный UI придёт на этапе 7 | Критерий этапа — «dev-samples индексируются и видны»; отладочный список подтверждает индексацию раньше, чем будет готов полноценный UI
2026-09-24 | Этап 4: `MusicEngine` — чистая конечная логика в `:core` над интерфейсом `PlayerHandle` (два слота A/B); ExoPlayer-реализация — тонкая обёртка в `:app` | Хореография кроссфейда покрывается unit-тестами с фейковыми плеерами и виртуальным временем (kotlinx-coroutines-test), без Robolectric и эмулятора
2026-09-24 | Дебаунс колеса — 600 мс «последнее значение»; гистерезис переключения KEEP_RADIUS = 0.4 (`WheelMusicController`), якорь хранится отдельно для разведки и боя | Медленная проводка маркера не должна спамить переключениями; режимы независимы, как в ТЗ
2026-09-24 | Кроссфейд equal-power (cos/sin), уходящий плеер стартует с текущей громкости; защита от наложения фейдов — счётчик поколений + mutex | Прерывание фейда новым переключением остаётся без щелчков и не теряет игроков; с парой A/B «осиротевших» слотов не бывает
2026-09-24 | Media3 вносит в манифест `ACCESS_NETWORK_STATE` и `WAKE_LOCK` | Штатные разрешения плеера (оценка bandwidth, wake lock для будущего сервиса); `INTERNET` по-прежнему отсутствует — приватная гарантия не нарушена, проверено `aapt2 dump permissions`
2026-09-24 | `MusicEngine` живёт в `AppContainer.appScope` (SupervisorJob + Default), переживает пересоздание Activity | Сервис и уведомление придут на этапе 9; до тех пор воспроизведение привязано к процессу, а не к экрану
2026-09-24 | Этап 5: общий `FadeCoordinator` (поколение + mutex) вынесен из MusicEngine и используется обоими движками; поколение бронируется синхронно `begin()` в момент заказа фейда | Иначе отложенный fade-in дочерней корутины мог «передавить» более поздний fade-out — реальный баг, найденный тестами
2026-09-24 | Бюджет окружения: 6 слотов = 1 base + 1 weather + 4 общих слота для LOOP-слоёв и спотов; споты занимают свободный слот только во время звучания и пропускаются, если слотов нет | Ручные слои важнее спотов; максимум 4 LOOP-слоя, лишние остаются выключенными
2026-09-24 | Смена день/ночь на одном base-слоте идёт «через тишину» (полкроссфейда вниз, смена источника, полкроссфейда вверх) | Полный A/B-кроссфейд дня/ночи потребовал бы 2 слота и сломал бюджет ≤8 ExoPlayer'ов; переключение редкое и осознанное
2026-09-24 | Фолбэк-лупер (`SeamlessFallbackLooper`, 2 доп. плеера) включается только когда meta.json объявил `seamless: false` И известна длительность base-файла | Штатный путь — REPEAT_MODE_ONE (бесшовно для корректных OGG/Opus); лупер подменяет хвост файла, перекрывая энкодерный зазор, и требует известной длительности для точки подмены
2026-09-24 | Панорамирование спотов отложено (джиттер pan из ТЗ не применён) | ExoPlayer не имеет прямого pan; SoundPool с pan придёт на этапе 6 для одиночных звуков; pitch-джиттер реализован через playback speed
2026-09-24 | Этап 6: владение фейдами в `FadeCoordinator` стало по-хендловым (`begin(хендлы)`), добавлен `rampAllTo` для одновременного рампа нескольких плееров | Найден реальный баг: единая глобальная генерация означала, что fade-in слоя «передавил» fade-in базы — окружение с base+слоем играло базу с громкостью 0; regression-тест `base and layer fade in together`
2026-09-24 | Ducking одиночными звуками: OneShotEngine глушит оба длинных движка на `duckingDb` (−3.5 дБ по умолчанию) и возвращает громкость по дедлайну `длительность файла + 300 мс хвост`; неизвестная длительность → 3 с | SoundPool не сообщает о конце стрима; длительности уже пробавляются индексатором; каждый новый выстрел переносит дедлайн, «Стоп все» возвращает громкость немедленно
2026-09-24 | Джиттер одиночных звуков (гейн/панорама/питч) применяется только если в meta.json задан объект `random`; без него звук играется ровно как проиндексирован | Сюрприз-джиттер по умолчанию для всех кнопок был бы неожиданным; споты окружения продолжают требовать `random` для планировщика
2026-09-24 | `OneShot.variants` — список `SoundFile` (не строк-URI): движку нужны длительности для дедлайна ducking; `RandomSpec.panJitter` теперь используется и спотами, и одиночными звуками | Параллельный массив длительностей хуже модели; ExoPlayer-pan остаётся невозможным, поэтому pan-джиттер реализован только в SoundPool (SoundPool.setVolume per-channel)
2026-09-24 | Pan-закон equal-power (`PanMath`): −1 → (1,0), +1 → (0,1), сумма квадратов ≈ 1 | Постоянная воспринимаемая громкость при повороте панорамы; центр = 0.707/0.707
2026-09-24 | `SoundPoolMixer`: кэш декодированных звуков по uri, первый тап ставит воспроизведение в очередь до `OnLoadCompleteListener`, fd закрывается сразу после декодирования; stop-all — по своим stream-id | Пере-декодирование каждого тапа дало бы задержку; SoundPool сам ограничивает число одновременных стримов (maxStreams=8), движок дедлайны хранит под синтетическими ключами для стримов «ещё грузится»
2026-09-24 | On-device (эмулятор, Android 15): ExoPlayerHandle постит все мутации на main-looper (зеркало громкости обновляется сразу у вызывающего, порядок команд сохраняется очередью Handler) | Эмулятор поймал «Player is accessed on the wrong thread»: командные циклы движков работают на Dispatchers.Default, а ExoPlayer разрешает доступ только с потока создания
2026-09-24 | SafScanner: для tree-URI используется `getTreeDocumentId`, а children-URI строится от ИСХОДНОГО granted tree (`buildChildDocumentsUriUsingTree(treeUri, childDocId)`) | `getDocumentId` на tree-URI кидает IllegalArgumentException (hasAccess всегда false → PermissionLost); пересборка tree-части с вложенным docId создаёт «чужое» дерево, на которое гранта нет — SecurityException на подпапках; оба бага пойманы только на устройстве
2026-09-24 | MainScreen: контент обёрнут в `windowInsetsPadding(WindowInsets.safeDrawing)` | edge-to-edge (enableEdgeToEdge) прижимал чипы под статус-бар; фундамент для этапа 7
2026-09-24 | rescan логирует результат (DnDSoundLibrary: N files / access lost / failed) | SAF-ошибки невозможно отличить без логов; воспроизводимость диагностики на реальных устройствах
2026-09-24 | Этап 7: adaptive-лейаут на material3-adaptive (`currentWindowAdaptiveInfoV2`, BOM 1.3.0) + собственный enum `LayoutMode`; `calculateWindowSizeClass` и `WindowWidthSizeClass` не используются (deprecated) | Правило «без deprecated API»; брейкпоинты через `isWidthAtLeastBreakpoint(WIDTH_DP_MEDIUM_LOWER_BOUND/WIDTH_DP_EXPANDED_LOWER_BOUND)` — константы окна, а не магические числа
2026-09-24 | Раскладка: compact — чипы + одна панель; medium — NavigationRail + панель; expanded — rail + три аудио-панели рядом (микшер и библиотека — одной панелью) | Колесо+окружение+звуки — основной рабочий набор ГМа; пять панелей рядом были бы тесными
2026-09-24 | Шины микшера (MASTER/MUSIC/AMBIENCE/SFX) хранятся в AppSettings (DataStore), все три движка получают setBusGains и умножают master×bus×duck в каждый целевой громкости | Сцены (этап 8) снапшотят те же BusGain-модели; звучание переживает перезапуск; Ducking остаётся поверх шин
2026-09-24 | Слайдеры шин коммитят значение на `onValueChangeFinished`, во время перетаскивания — локальное состояние | Иначе каждое движение пишет DataStore и пересылает setBusGains в движки ~60 раз/сек
2026-09-24 | Обнаружен и исправлен корень «чёрного текста на тёмном фоне»: контент не был обёрнут в Surface, все plain `Text` брали дефолтный `LocalContentColor = чёрный` | MaterialTheme не задаёт LocalContentColor; Surface в DnDSoundTheme ставит onBackground всем текстам сразу
2026-09-24 | Таверн-палитра AMBER — тема по умолчанию (DARK остался переключалкой); serif-типографика для заголовков (системный Serif, без файлов шрифтов в репо), тёплый градиент-фон, цветные сектора колеса | Обратная связь по читаемости и виду; системные шрифты не тянут лицензии/вес в APK
2026-09-24 | material-icons-extended 1.7.8 (BOM-pinned) для rail/иконок темы | В core-наборе нет MusicNote/Forest/Tune; в release иконки вырезаются R8
2026-09-24 | СТРОГОЕ правило секторов (обратная связь): `TrackSelector` выбирает только из сектора точки — спокойствие = calm-папка, настроения = треки своей папки; пустой сектор = тишина, никакого фолбэка на «3 ближайших» | Тап по Спокойствию включал хоррор: старый селектор фильтровал только по режиму и брал ближайшие; удалён NEAREST_FALLBACK и R_MAX
2026-09-24 | Автопереход в конце трека: новый `TrackSelector.next(current)` строит пул из сектора ТЕКУЩЕГО трека; MusicEngine получил `selectNextTrack` (дефолт — повтор того же трека) | Пул от позиции трека, а не якоря — якорь на границе сектора не даёт утечь; on-device: 2-сек треки, после N автопереходов заголовок остался «creepy»
2026-09-24 | Кнопка «Следующий» удалена (строка music_next тоже) | Обратная связь: колесо — единственный контрол выбора; автопереход конца трека остался в движке
2026-09-24 | Избранные звуки сортируются наверх (`OneShotDisplay.sortedForDisplay`, core, юнит-тесты) | Рабочие звуки ГМа должны быть в пределах большого пальца
2026-09-24 | Панели этапа 7 скроллятся (verticalScroll), колесо ограничено widthIn(max=440dp), все чипы heightIn(min=48dp) | На телефоне без скролла FlowRow звуков уходил за экран; на планшете колесо растягивалось до абсурда
2026-09-24 | Дополнение к промпту реализовано отдельным заходом «зоны колеса» (25 зон: neutral + 8×2 ступени + 8 переходов); константы WheelConfig: центр 0.20, деление ступеней 0.60, полоса перехода ±6°, гистерезис 0.03 | Спецификация пользователя; KEEP_RADIUS/R_MAX удалены, WheelMusicController удалён — решение о смене трека принимает MusicEngine по зоне с гистерезисом
2026-09-24 | Источник правды зон — core/src/main/resources/wheel_zones.json (id, folder, цвет, between), грузится classloader'ом в рантайме; проверено, что AGP упаковывает ресурсы JVM-модуля в APK (unzip -l) | Переименование зон/цветов не требует правок Kotlin; юнит-тест round-trip id↔папка↔зона
2026-09-24 | Фолбэк на пустую зону ВЕРНУЛСЯ (спец-дополнение пользователя заменяет «строгое» правило секторов из фидбека этапа 7): пустая зона играет ближайшую непустую той же моды, но теперь ЯВНО — чип «Играет: <зона>»; чип привязан к targetZone != playingZone, поэтому живёт и после автопереходов | 25 мелких зон невозможно заполнить все; тихая утечка старого селектора была багом, явная подпись — фича
2026-09-24 | Градиент колеса: wheelColorAt — OKLab-интерполяция 16 якорей (8 секторов + 8 переходов) со smoothstep, mix(r): 0 при r≤0.10 → 0.55 при 0.60 → 1.0; рисуется один раз в ImageBitmap на Dispatchers.Default, кэш по размеру/пустым зонам | Спецификация раздела G; Compose-градиенты не дают OKLab и радиального приглушения к нейтрали
2026-09-24 | Позиция по умолчанию — детерминированный FNV-хэш пути: сектор ±14.5°, INNER r∈[0.28,0.52], OUTER [0.68,0.92]; переход ±4.5° r∈[0.30,0.90]; neutral r∈[0.02,0.16]; property-тест 1000 сидов на зону подтверждает попадание в свою зону | Спецификация раздела I; ручная позиция (Room overrides) имеет приоритет и переживает рескан
2026-09-24 | Неизвестная папка → трек «не размещён» (position=null, не играет) + предупреждение UNKNOWN_MUSIC_FOLDER; в Библиотеке секция «Не размещены» с диалогом «Выбрать зону» (25 зон, сгруппированы); «Разместить на колесе» перетаскиванием отложено (в дополнении — вариант «или») | Диалог удобнее на телефоне; драг-пин — кандидат в этап 10
2026-09-24 | meta.json: поле mood заменено на zone (id из wheel_zones.json); wheel x/y остался; приоритет: ручная > wheel > zone > папка | Обратная совместимость с mood не нужна — формат наш, конвенция новая
2026-09-24 | Названия зон продублированы английским в values/ (по умолчанию) и values-en/ — пользователь переведёт values/ сам («переведу сам» в дополнении) | Остальные строки UI — ru по умолчанию + values-en
2026-09-24 | Пустые зоны приглушаются десатурацией 35% в растровом градиенте; настройка highlight_empty_zones (DataStore, по умолчанию вкл), UI-переключатель — в этапе 10; если музыки в библиотеке нет — ничего не гасится | Спецификация раздела G
2026-09-24 | Колесо: двойной тап → центр, хаптик-тик при смене зоны (TextHandleMove), TalkBack stateDescription = название зоны + 4 custom actions (по/против часовой, наружу/к центру) —TalkBack сделан раньше этапа 10, т.к. тривиален поверх готовой геометрии | Спецификация раздела H
2026-09-24 | Удалён мёртвый core/audio/AudioEngine.kt (EngineState/AudioEngine не реализуются никем со времён этапа 2) | Мёртвый код с isCalm сломал компиляцию после удаления CALM_RADIUS
2026-09-24 | «Создать структуру» теперь создаёт полное дерево зон (разведка+бой, 60+ папок) через ensureDir (поиск существующей подпапки → createDocument) | Пользователь больше не вручную создаёт ступени/переходы
